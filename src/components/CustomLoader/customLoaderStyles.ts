import {StyleSheet} from 'react-native-size-scaling';
export const customLoaderStyles = StyleSheet.create({
  loader: {
    paddingHorizontal: 48,
    alignItems: 'center',
    justifyContent: 'center',
    flex: 1,
  },
});
